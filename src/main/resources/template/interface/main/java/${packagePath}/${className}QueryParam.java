package ${packagePath};

import net.sinedu.company.bases.PagingAdapter;
import net.sinedu.company.bases.QueryParamAdapter;

/**
 * ${comments}
 *
 * @author ${author}
 * @email ${email}
 * @date ${datetime}
 */
public class ${className}QueryParam extends QueryParamAdapter {

    public ${className}QueryParam() {
    }

    public ${className}QueryParam(PagingAdapter pagingAdapter) {
        super(pagingAdapter);
    }
}
